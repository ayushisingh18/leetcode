//Add Digits-258
class Solution {
public:
    int addDigits(int num) {
       while(num>9)
       {
        int sum=0,n;
        while(num!=0)
        {
            int n=num%10;
            num/=10;
            sum=sum+n;
        }
        num=sum;
       }
       return num;
    }
};
